const paymentPage=async()=>{const box=document.querySelector('#order');if(!box)return;const id=new URLSearchParams(location.search).get('id');const [order,config]=await Promise.all([window.shopOrder||fetch('/api/orders/'+id).then(r=>r.ok?r.json():null),fetch('/api/payments/config').then(r=>r.json())]);if(!order)return;box.insertAdjacentHTML('beforeend','<hr><div id="payment-methods"></div><div id="payment-agreement"></div><button id="pay-button" class="cta">결제하기</button><p id="payment-message"></p>');if(!config.clientKey){document.querySelector('#payment-message').textContent='결제 테스트 키가 설정되지 않았습니다.';return}const script=document.createElement('script');script.src='https://js.tosspayments.com/v2/standard';script.onload=async()=>{try{const me=await (window.shopMe||fetch('/api/me').then(r=>r.json()));const widgets=TossPayments(config.clientKey).widgets({customerKey:me.id});await widgets.setAmount({currency:'KRW',value:Number(order.total)});await widgets.renderPaymentMethods({selector:'#payment-methods'});await widgets.renderAgreement({selector:'#payment-agreement'});document.querySelector('#pay-button').onclick=()=>widgets.requestPayment({orderId:order.id,orderName:order.items.map(x=>x.name).join(', '),successUrl:location.origin+'/payment-success',failUrl:location.origin+'/payment-fail'});}catch(e){document.querySelector('#payment-message').textContent='결제창을 불러오지 못했습니다.'}};document.head.appendChild(script)};
window.addEventListener('shop:order-rendered',paymentPage,{once:true});
